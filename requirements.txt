
# ── Django Core ──────────────────────────────────────────────
Django==6.0.6
psycopg[binary]==3.3.4            # PostgreSQL adapter (v3, async-ready)
python-dotenv==1.2.2              # .env file loading
Pillow==11.2.1                    # Image processing (required by ImageField)

# ── Django REST Framework & Extensions ───────────────────────
djangorestframework==3.17.1
django-filter==25.1               # Queryset filtering for DRF
django-cors-headers==4.6.0        # CORS middleware

# ── Clerk Auth & Webhooks ──────────────────────────────────
PyJWT==2.10.1                     # JWT decoding & JWKS verification
cryptography>=44.0.0              # RSA key support for JWKS
svix==1.62.0                      # Clerk webhook signature verification
requests==2.32.3                  # HTTP library for fetching JWKS

# ── Async / WebSocket (ASGI) ─────────────────────────────────
channels==4.3.2                   # WebSocket & async support
daphne==4.2.2                     # ASGI server (HTTP + WS)

# ── Task Queue ───────────────────────────────────────────────
celery==5.6.3                     # Distributed task queue
redis==8.0.1                      # Redis client (broker + cache)

# ── Phase 5: Resume Intelligence ─────────────────────────────
pdfplumber==0.11.6                # PDF text extraction
python-docx==1.1.2                # DOCX text extraction
pytesseract==0.3.13               # OCR fallback for scanned PDFs
pdf2image==1.17.0                 # PDF → image for OCR rasterization
python-magic-bin==0.4.14          # File type sniffing (bundles libmagic for Windows)
google-genai==1.14.0              # Google Gemini API SDK
google-auth>=2.27.0               # Required sub-dependency for Google GenAI SDK
sentence-transformers==4.1.0      # SBERT embeddings for matching
django-storages==1.14.4           # S3 storage backend (non-dev)
boto3==1.36.26                    # AWS SDK for S3

# ── Dev / Typing ─────────────────────────────────────────────
django-stubs==5.2.0               # Type stubs for Django ORM
djangorestframework-stubs==3.16.0 # Type stubs for DRF
drf-spectacular==0.28.0           # OpenAPI 3.0 schema + Swagger UI
