
# ── Django Core ──────────────────────────────────────────────
Django==6.0.6
psycopg[binary]==3.3.4            # PostgreSQL adapter (v3, async-ready)
python-dotenv==1.2.2              # .env file loading
Pillow==11.2.1                    # Image processing (required by ImageField)

# ── Django REST Framework & Extensions ───────────────────────
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.0  # JWT authentication
django-filter==25.1               # Queryset filtering for DRF
django-cors-headers==4.6.0        # CORS middleware

# ── Async / WebSocket (ASGI) ─────────────────────────────────
channels==4.3.2                   # WebSocket & async support
daphne==4.2.2                     # ASGI server (HTTP + WS)

# ── Task Queue ───────────────────────────────────────────────
celery==5.6.3                     # Distributed task queue
redis==8.0.1                      # Redis client (broker + cache)

# ── Dev / Typing ─────────────────────────────────────────────
django-stubs==5.2.0               # Type stubs for Django ORM
djangorestframework-stubs==3.16.0 # Type stubs for DRF
drf-spectacular==0.28.0           # OpenAPI 3.0 schema + Swagger UI
